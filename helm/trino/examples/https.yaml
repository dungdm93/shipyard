httpServer:
  https:
    enabled: true
    port: 8443

ingress:
  enabled: true
  hosts:
    - host: trino.local
      paths: [/]
  tls:
  - secretName: trino.local-tls
    hosts:
      - trino.local
